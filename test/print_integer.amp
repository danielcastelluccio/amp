use "std.amp";

fn main() {
	@print_memory();
    test();
	@print_memory();
};

fn test() {
    println("a" + 35 + 34);
};

fn _+(value1: String, value2: integer): String {
    return value1 + integer_to_string(value2);
};
