use "std"

fn main(arguments: Array<String>) {
    let i = 0
    while i < arguments.length {
        let argument = arguments[i]
        println(argument + ", size: " + argument.length.to_string())
        i = i + 1
    }
}
