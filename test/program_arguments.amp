use "std.amp";

fn main(arguments: Array) {
    let i = 0;
    while (less(i, arguments.length())) {
        let argument = String(arguments.get(i));
        println(concatenate(argument, concatenate(", size: ", integer_to_string(argument.length()))));
        i = i + 1;
    };
};