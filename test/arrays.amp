use "std"

fn main() {
    let array = Array<String>(2)
    array[0] = "Hello "
    array[1] = "World!"
    println(array[0] + array[1])
    println("Array Length: " + integer_to_string(array.length))
}
