use "std.amp";

fn main() {
    let array = Array.new(1, String.memory_size());
    array.set(0, "/bin/ls");

    execute_command("/bin/ls", array, true);

    println("after first!");

	array = Array.new(2, String.memory_size());
	array.set(0, "/bin/ls");
	array.set(1, "-l");

    execute_command("/bin/ls", array, true);

    println("after second!");
};
