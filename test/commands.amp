use "std.amp";

fn main2() {
    execute_command("/bin/ls", Array.new(0), true);
};

fn main() {
    let array = Array.new(1);
    array.set(0, "/bin/ls");

    execute_command("/bin/ls", array, true);

    println("after first!");

    let list = LinkedList.new();
    list.append("/bin/ls");
    list.append("-l");
    array = list.to_array();
    
    execute_command("/bin/ls", array, true);

    println("after second!");
};
