use "std.amp";

fn main() {
    let parameters = Array.new(1);
    parameters.set(0, "Welcome child!");

    let functions = Array.new(2);
    functions.set(0, test);
    functions.set(1, test2);

    let i = 0;
    while (i < functions.length()) {
        println(@cast_String(@cast_Function(functions.get(i)).call(parameters)));
        i = i + 1;
    };
};

fn test(test: String) {
    return "Hello from test function! Main function says: " + test;
};

fn test2(test: String) {
    return "Hello from test3 function! Main function says: " + test;
};