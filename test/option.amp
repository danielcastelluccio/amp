use "std.amp";

fn main() {
    let option = Option.empty();

    println(boolean_to_string(Option.is_value(option)));

    option = Option.of("Hello Option!");
    if (Option.is_value(option)) {
        println(String(Option.data(option)));
    } else {
        println("not a thing (this should never print with this code)");
    };
};